version: "3"
services:
  nginx:
    build:
      context: ./nginx
      dockerfile: nginx_Dockerfile
    ports:
      - 80:80
    depends_on:
      - docker-springboot
  docker-springboot:
    build: .
    expose:
      - 8080